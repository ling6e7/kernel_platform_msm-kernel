name: Build Xiaomi 13 Rebased Kernel (Fixed v2)

on:
  workflow_dispatch:
    inputs:
      UPLOAD_NAME:
        description: '固件命名后缀'
        required: true
        default: 'Fuxi-Rebased'

jobs:
  build:
    runs-on: ubuntu-24.04
    
    steps:
    - name: 1. 拉取代码
      uses: actions/checkout@v4
      with:
        ref: staging-lineage-2
        fetch-depth: 1
  
    - name: 2. 安装基础依赖 (适配 Ubuntu 24.04)
      run: |
        sudo apt-get update
        # 移除了 lib32ncurses5-dev/libncurses5，改用 libncurses-dev 和 lib32ncurses-dev
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf imagemagick lib32readline-dev lib32z1-dev liblz4-tool libncurses-dev lib32ncurses-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3 python3-pip python3-dev device-tree-compiler jq libfuse2

    - name: 3. 修复损坏的 KernelSU 子模块
      run: |
        echo "正在修复 broken submodule..."
        # 强行绕过 build.sh 中的 git submodule 错误
        sed -i 's/git submodule init && git submodule update/echo "Skipping broken submodule update"/' build.sh
        
        # 手动拉取官方 KernelSU
        rm -rf KernelSU
        git clone https://github.com/tiann/KernelSU KernelSU
        echo "✅ KernelSU 手动修复完成"

    - name: 4. 设置 Java 17 环境 (为 Re-Kernel 准备)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 5. 自动集成 Re-Kernel
      run: |
        echo "下载并运行 Kernel Modifier..."
        ASSET_URL=$(curl -s https://api.github.com/repos/Sakion-Team/Re-Kernel/releases/tags/modifier | jq -r '.assets[0].browser_download_url')
        wget $ASSET_URL -O KernelModifier.jar
        
        # 运行修改器
        java -jar KernelModifier.jar     
   
    - name: 6. 准备编译器
      run: |
        mkdir -p toolchains/clang
        cd toolchains/clang
        curl -LO "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman"
        chmod +x antman
        ./antman -S
        
    - name: 7. 执行编译
      run: |
        export PATH=$(pwd)/toolchains/clang/bin:$PATH
        chmod +x build.sh
        
        echo "=== 开始运行修复后的 build.sh ==="
        bash build.sh

    - name: 8. 搜集并上传产物
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-${{ inputs.UPLOAD_NAME }}
        path: |
          *.zip
          out/*.zip
          AnyKernel3/*.zip
          out/arch/arm64/boot/Image
